<% flash.each do |message_type, message| %>
  <% if message_type = 'notice' %>
    <div class="alert alert-success"><%= message %></div>
  <% else %>
    <div class="alert alert-success"><%= message %></div>
  <% end %>
<% end %>